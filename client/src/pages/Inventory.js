import React from 'react';
import AdminPanel from '../components/AdminPanel';

const Inventory = () => {
    return (
        <div>
            <AdminPanel/>
        </div>
    );
};

export default Inventory;